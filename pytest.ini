[pytest]
testpaths = tests
addopts = --cov=src --cov-report=term-missing
markers =
    regression: pins known-good numeric results (W14 Regression Testing)
    slow: marks slow tests that may be skipped in fast CI runs
